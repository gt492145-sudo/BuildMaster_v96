/* BuildMaster calc compatibility bundle.
   index.html loads this file after bm-blueprint.js. The current modular build
   keeps the calculation engine in scripts/features/calc-engine.js. */
console.info('BuildMaster calc module loaded from features/calc-engine.js');
